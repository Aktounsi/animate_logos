from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='We build a model, that automatically animates SVG Logos.',
    author='Teamproject University of Mannheim',
    license='',
)
